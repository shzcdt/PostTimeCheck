# PostSpy - Telegram-бот для анализа каналов

PostSpy - это мощный инструмент для анализа Telegram-каналов, позволяющий собирать и экспортировать данные о постах в формате Excel. Бот использует API Telegram для получения информации о сообщениях, просмотрах, комментариях и реакциях.

## 🚀 Возможности

- 📊 Анализ нескольких Telegram-каналов одновременно
- 📅 Сбор постов за различные периоды (неделя, месяц, все время или custom период)
- 📈 Экспорт данных в Excel-файлы с подробной информацией
- 📝 Сбор следующих метрик:
  - Текст сообщения
  - Дата и время публикации
  - Количество просмотров
  - Количество комментариев
  - Количество реакций
  - ID сообщения
  - Название канала

## 🛠 Технологии

- **Python** - основной язык программирования
- **python-telegram-bot** - фреймворк для создания Telegram-ботов
- **Telethon** - библиотека для взаимодействия с Telegram API
- **SQLite** - база данных для хранения собранных данных
- **openpyxl** - библиотека для работы с Excel-файлами
- **python-dotenv** - управление переменными окружения

## 📋 Установка и настройка

### 1. Клонирование репозитория

```bash
git clone <your-repository-url>
cd postspy
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта со следующими параметрами:

```env
BOT_TOKEN=ваш_токен_бота_от_BotFather
API_ID=ваш_API_ID_от_Telegram
API_HASH=ваш_API_HASH_от_Telegram
```

**Как получить эти данные:**
- `BOT_TOKEN` - создайте бота через [@BotFather](https://t.me/BotFather) в Telegram
- `API_ID` и `API_HASH` - зарегистрируйте приложение на [my.telegram.org](https://my.telegram.org/auth)

### 4. Запуск бота

```bash
python main.py
```

## 💡 Использование

1. **Запустите бота** - отправьте команду `/start`
2. **Укажите каналы** - введите ссылки на Telegram-каналы в одном из форматов:
   - `@channelname`
   - `https://t.me/channelname`
   - Можно указать несколько через запятую или с новой строки
3. **Выберите период** - укажите временной диапазон для сбора постов:
   - `/last_week` - посты за последнюю неделю
   - `/last_month` - посты за последний месяц
   - `/all` - все доступные посты (до 1000)
   - `/custom` - указать произвольное количество дней
4. **Укажите лимит** - определите максимальное количество постов для сбора с каждого канала
5. **Получите результат** - бот отправит Excel-файл с собранными данными

## 🏗 Структура проекта

```
postspy/
├── main.py                 # Основной файл запуска бота
├── handlers.py             # Обработчики команд бота
├── telethon_client.py      # Инициализация клиента Telegram
├── requirements.txt        # Зависимости проекта
├── .env                    # Переменные окружения (не включены в репозиторий)
├── postspy.db              # SQLite база данных (генерируется автоматически)
├── utils/
│   ├── database.py         # Работа с базой данных
│   └── message_parser.py   # Парсинг сообщений из Telegram
└── *.xlsx                  # Экспортированные файлы (генерируются автоматически)
```

## 🔧 Команды бота

- `/start` - начать новый анализ
- `/help` - показать справку
- `/cancel` - отменить текущий процесс
- `/test <канал>` - протестировать доступ к каналу

## 📁 Формат экспорта

Excel-файл содержит следующие колонки:
- `id` - внутренний ID записи
- `channel` - название канала
- `text` - текст сообщения
- `date` - дата и время публикации
- `views` - количество просмотров
- `comments_count` - количество комментариев
- `reactions_count` - количество реакций

## ⚠️ Ограничения

- Поддерживаются только **публичные** Telegram-каналы
- Максимальный лимит постов - 1000 на канал
- Максимальный период - 365 дней
- Некоторые каналы могут ограничивать доступ к данным через API

## 🤝 Вклад в проект

Если вы хотите внести свой вклад в развитие проекта:

1. Сделайте форк репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Зафиксируйте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Отправьте изменения (`git push origin feature/AmazingFeature`)
5. Создайте pull request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. Смотрите файл `LICENSE` для получения дополнительной информации.

## 🆘 Поддержка

Если у вас возникли вопросы или проблемы с использованием бота, вы можете:

- Создать issue в репозитории
- Обратиться к команде разработчиков

---

<div align="center">
  <sub><em>PostSpy - делает анализ Telegram-каналов простым и эффективным!</em></sub>
</div>